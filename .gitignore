/rag_env/
/chat.db

# Secrets — never commit real API keys
/groqkey.txt
session_secret.txt

# Runtime artifacts — regenerated, not needed to run the app
__pycache__/
/uploads_tmp/
/eval_results_*.xlsx
